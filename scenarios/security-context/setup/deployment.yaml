apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  replicas: 1
  selector: {}
  template:
    metadata:
      labels: {}
    spec:
      containers:
      - name: webapp
        image: nginx:alpine
        ports:
        - containerPort: 8080
